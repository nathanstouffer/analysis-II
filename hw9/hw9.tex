\documentclass[11pt]{article}

\usepackage{../analysis}

\begin{document}

\coverpage{9}

% hw problem 1 -----------------------------------------------------------------

\newpage
\section*{Problem 1}
    \problem{
        Let $f: \R ^2 \to \R$ be defined by
        \[
        f(x,y) =
        \begin{cases}
            \frac{xy(x^2 - x^2)}{x^2 + y^2} & (x,y) \neq (0,0) \\
            0                               & (x,y) = (0,0)
        \end{cases}
        \]
        {\bf (a)} Show that $\partial f / \partial x$, $\partial f / \partial y$ exist for all $(x,y) \in \R ^2$ \\\\
        {\bf (b)} Show that both $\frac{\partial ^2 f(0,0)}{\partial x \partial y}$ and $\frac{\partial ^2 f(0,0)}{\partial y \partial x}$ exist but $\frac{\partial ^2 f(0,0)}{\partial x \partial y} \neq \frac{\partial ^2 f(0,0)}{\partial y \partial x}$
    }
    \proof{

    }

% hw problem 2 -----------------------------------------------------------------

\newpage
\section*{Problem 2}
    \problem{
        For any $x = (x_1, \ldots , x_n) \in \R ^n$ and any multi-index $\alpha = (\alpha _1, \ldots , \alpha _n)$, prove that $|x^\alpha| \leq |x|^{|\alpha|}$ where $x^\alpha = x_1^{\alpha _1} \cdots x_n^{\alpha _n}$, $|x| = \sqrt{x_1^2 + \cdots + x_n^2}$, and $|\alpha| = \alpha _1 + \cdots + \alpha _n$
    }
    \proof{

    }

\end{document}
